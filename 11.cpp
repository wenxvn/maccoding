#include<bits/stdc++.h>
using namespace std;
#define int long long
int n,q;
vector<int> a;
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin>>n>>q;
    a.resize(n);
    for(int i=0;i<n;i++)cin>>a[i];
    while(q--){
        int l,r;cin>>l>>r;
        l--;r--;
        
    }
    return 0;
}